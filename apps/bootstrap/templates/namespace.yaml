apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    managed-by: argocd
    trino-deployment: {{ .Values.deploymentName }}
    cluster: {{ .Values.clusterName }}

# Made with Bob
